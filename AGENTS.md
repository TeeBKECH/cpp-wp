# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/placeholder project. It contains a single file (`test`) with no application code, build system, or dependencies.

- **No programming language or framework** has been established yet.
- **No package manager lockfile** exists — when one is added, use the matching package manager (`package-lock.json` → npm, `yarn.lock` → yarn, `pnpm-lock.yaml` → pnpm, `bun.lockb` → bun, `requirements.txt` / `pyproject.toml` → pip/uv).
- **No services** need to be started.
- **No lint, test, or build commands** are available.

When application code is added, update this file with relevant setup, run, and testing instructions.
